# We have to do an underscore here because rust's `tr!` macro uses the internal macro
# `module_path!` which will take our name in `Cargo.toml` and replace the `-` with `_`.
i18n.gettext('hello_rhino', preset: 'glib', install_dir: po_dir)
